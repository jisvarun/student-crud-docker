services:
  app:
    image: php:8.2-apache

    container_name: hostel-app

    ports:
      - "8000:80"

    volumes:
      - ./:/var/www/html

    restart: unless-stopped

    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.hostel.rule=Host(`hostelcampus.com`)"
      - "traefik.http.routers.hostel.entrypoints=websecure"
      - "traefik.http.routers.hostel.tls=true"
      - "traefik.http.routers.hostel.tls.certresolver=letsencrypt"
      - "traefik.http.services.hostel.loadbalancer.server.port=80"

    networks:
      - traefik

networks:
  traefik:
    external: true